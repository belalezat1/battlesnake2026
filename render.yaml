services:
  - type: web
    name: packet-sniffers
    runtime: python
    region: ohio
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 10
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.0"
